[project]
name = "hexenbracken"
version = "0.9.0"
description = "Python scripts to google sheets into hexcrawls for D&D."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "jinja2>=3.1.6",
]
